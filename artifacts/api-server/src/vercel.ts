// Vercel Function entry: bare Express app, no app.listen() call. Vercel's
// Node.js runtime wraps this exported app to handle requests — see
// /api/index.ts at the repo root for the actual Function file, and
// docs/DEPLOYMENT.md for how the two are wired up.
export { default } from "./app";
